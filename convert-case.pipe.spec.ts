import { ConvertCasePipe } from './convert-case.pipe';

describe('ConvertCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertCasePipe();
    expect(pipe).toBeTruthy();
  });
});
